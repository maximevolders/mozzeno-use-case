export interface ComputedProductModel {
    id: string;
    duration: number;
    min_rate: number;
    max_rate: number;
    min_instalment_amount: number;
    max_instalment_amount: number;
    min_total_payment: number;
    max_total_payment: number;
}